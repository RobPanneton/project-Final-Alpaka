const initialState = {
  home: null,
  events: null,
  artists: null,
  releases: null,
  about: null,
};

export const contentReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
